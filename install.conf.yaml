- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.shell:
    ~/.tmux.conf: 
    ~/.vimrc: 
    ~/.vim:
      force: true
    ~/.zshrc: 
    ~/.oh-my-zsh:
    ~/.gitconfig:
    ~/.tmux: 

# - create:
#     - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
